<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .mainDiv{
            width: 90%;
            height: auto;
            border: 1px solid black;
            margin: auto;
            margin-top: 20px;
            padding: 30px;
            display: flex;
        }
        .left{
            width: 60%;
            height: 90%;
            
            box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;
        }
        .right{
             width: 30%;
            height: 400px;
            position: fixed;
            right: 20px;
            box-shadow: rgba(0, 0, 0, 0.35) 0px 5px 15px;
        }
        .products{
            width: 90%;
            height: 90%;
            margin: auto;
            border: 1px solid green;
            margin-top: 10px;
            display: flex;
            padding: 30px;
            gap: 160px;

        }
        .products img{
            width: 200px;
            height: 200px;
        }
        .remove{
            width: 150px;
            height: 50px;
        }
        .container{
            padding: 50px;
            margin-top: 60px;
            background-color: white;
            border: none;
           
        }
        .container a {
            color: black;
            text-decoration: none;

        }
    </style>
</head>
<body>

    <div class="mainDiv">
            <div class="left">
                {{#each product}}
                    <div class="products">
                            <img src="/images/product/{{this.img}}" class="card-img-top" alt="...">
                             <div class="contet">
                                <h4 class="text-uppercase">{{this.Name}}</h4>
                                <p class="card-text">{{this.des}}</p>
                                <p class="card-text">{{this.price}}</p>
                                {{!-- <a href="#" class="btn btn-primary">buy now</a> --}}
                                <a href="/addtocart/{{this.id}}" class="btn btn-primary remove">remove</a>
                                <h6>quantity</h6>
                                <a href="/addqnty/{{this.id}}" class="btn qwe btn-outline-success">+</a>
                                <a href="/subqnty/{{this.id}}" class="btn qwe btn-outline-success">-</a>
                             </div>
                    </div>
                 {{/each}}
            </div>
            <div class="right">
                    <div class="container">

                        <p><a href="#">Total</a> <span class="price"> ₹{{user.total}}</span></p>
                        <p><a href="#">GST</a> <span class="price">₹{{user.GST}}</span></p>
                        <p><a href="#"> subTotal</a> <span class="price">₹{{user.subTotal}}</span></p>
                        <a href="/createOrder/{{user.subTotal}}" class="btn btn-success">checkout</a>
                        </div>
            </div>
    </div>
</body>
</html>